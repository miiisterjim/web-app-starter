After each step, commit your changes to source control with a commit message of `Exercise X` where `X` is the exercise number

1. Fix the broken api test
1. Fix the broken frontend test
1. Use a console.log statement to print the value of the response before it is returned to the browser (in the API)
1. Use a debug breakpoint to inspect the value of the response before it is sent back to the browser, and alter/edit the value that is returned
1. Use a console.log statement to print the value of the response in the browser console before it is displayed to the user
1. Use a debug breakpoint in the browser to inspect the value of the response before it is displayed to the user
1. Update the API so that it accepts a Query String value and returns "Hello {value}!" e.g. Hello Owen! If no Query String value is set then continue to return "Hello World!". Make sure we have tests to confirm it's working as we expect
1. Update the frontend to take the user entered value and pass that to the API and render the response